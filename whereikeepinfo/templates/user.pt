<div metal:use-macro="load: base.pt">
    <tal:block metal:fill-slot="content">
        <h1>${user.name}</h1>
        <p>login: <b class="byline">${user.username}</b>.</p>
        <p>email: <b class="byline">${user.email}</b>.</p>
        <h3 class="subtitle">Last Probe</h3>
        <p>Timestamp: <b class="hits">${user.last_probe}</b>.</p>
    </tal:block>
</div>
